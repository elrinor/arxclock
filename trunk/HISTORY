1.1.4
! Minor interface tweaks.

1.1.3
! Minor code cleanup.
* Now double-click on system tray icon brings main window to front even if it 
  was previously minimized.

1.1.2
+ Space toggles alarm disabled/enabled state (in main window).

1.1.1
+ Now it's possible to execute a shell command when alarm rings.

1.1.0
+ Multiple alarms can now run simultaneously (this prevents the situation when 
  a noiseless alarm blocks the noisy one).

1.0.0
* Initial release